var userController = require("../api/controllers/UserController");

describe("UserController", function() {
    it("should get one user when an id is set in the params", function() {
        expect(true).toBe(true);
    });
});;